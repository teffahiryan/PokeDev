{% extends 'base.html.twig' %}

{% block title %}CatchPokemon index{% endblock %}

{% block body %}
    <h1>CatchPokemon index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for catch_pokemon in catch_pokemons %}
            <tr>
                <td>{{ catch_pokemon.id }}</td>
                <td>
                    <a href="{{ path('catch_pokemon_show', {'id': catch_pokemon.id}) }}">show</a>
                    <a href="{{ path('catch_pokemon_edit', {'id': catch_pokemon.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('catch_pokemon_new') }}">Create new</a>
{% endblock %}
