{% extends 'base.html.twig' %}

{% block title %}CatchPokemon{% endblock %}

{% block body %}
    <h1>CatchPokemon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ catch_pokemon.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('catch_pokemon_index') }}">back to list</a>

    <a href="{{ path('catch_pokemon_edit', {'id': catch_pokemon.id}) }}">edit</a>

    {{ include('catch_pokemon/_delete_form.html.twig') }}
{% endblock %}
