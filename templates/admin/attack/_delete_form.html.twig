<form method="post" action="{{ path('attack_delete', {'id': attack.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ attack.id) }}">
    <button class="btn">Delete</button>
</form>
